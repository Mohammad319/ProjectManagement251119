@using Microsoft.AspNetCore.Identity

@inject SignInManager<ApplicationUser> SignInManager

<nav class="pm-side-nav" aria-label="Manage account navigation">
    <NavLink class="pm-side-link" href="Account/Manage" Match="NavLinkMatch.All">Profile</NavLink>
    <NavLink class="pm-side-link" href="Account/Manage/Email">Email</NavLink>
    <NavLink class="pm-side-link" href="Account/Manage/ChangePassword">Password</NavLink>
    @if (hasExternalLogins)
    {
        <NavLink class="pm-side-link" href="Account/Manage/ExternalLogins">External logins</NavLink>
    }
    <NavLink class="pm-side-link" href="Account/Manage/TwoFactorAuthentication">Two-factor authentication</NavLink>
    <NavLink class="pm-side-link" href="Account/Manage/Passkeys">Passkeys</NavLink>
    <NavLink class="pm-side-link" href="Account/Manage/PersonalData">Personal data</NavLink>
</nav>

@code {
    private bool hasExternalLogins;

    protected override async Task OnInitializedAsync()
    {
        hasExternalLogins = (await SignInManager.GetExternalAuthenticationSchemesAsync()).Any();
    }
}
