@inherits LayoutComponentBase

<div class="@BuildClasses()">
    @if (!string.IsNullOrWhiteSpace(Title))
    {
        <div class="@($"mhd-card-title {PaddingClass}")">
            @Title
        </div>
    }

    @if (Header is not null)
    {
        <div class="@($"mhd-card-header {PaddingClass}")">
            @Header
        </div>
    }

    <div class="@PaddingClass">
        @ChildContent
    </div>

    @if (Footer is not null)
    {
        <div class="@($"mhd-card-footer {PaddingClass}")">
            @Footer
        </div>
    }
</div>

@code {
    public enum CardInteractionMode
    {
        None,
        HoverOnly,
        RippleOnly,
        HoverAndRipple
    }

    [Parameter] public string? Title { get; set; }
    [Parameter] public RenderFragment? Header { get; set; }
    [Parameter] public RenderFragment? ChildContent { get; set; }
    [Parameter] public RenderFragment? Footer { get; set; }
    [Parameter] public string? Class { get; set; }

    [Parameter] public EventCallback OnClick { get; set; }
    [Parameter] public CardInteractionMode Interaction { get; set; } = CardInteractionMode.None;
    [Parameter] public bool Elevated { get; set; } = false;
    [Parameter] public bool Compact { get; set; } = false;

    private string PaddingClass => Compact ? "mhd-card-pad-compact" : "mhd-card-pad-regular";

    private string BuildClasses()
    {
        var cls = new List<string>
        {
            "mhd-card-base",
            "mhd-transition-normal"
        };

        if (Elevated)
            cls.Add("mhd-card-elevated");

        if (Interaction == CardInteractionMode.HoverOnly ||
            Interaction == CardInteractionMode.HoverAndRipple)
        {
            cls.Add("mhd-card-interactive");
        }

        if (!string.IsNullOrWhiteSpace(Class))
            cls.Add(Class);

        return string.Join(" ", cls);
    }
}
