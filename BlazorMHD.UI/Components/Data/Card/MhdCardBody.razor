@code {
    [CascadingParameter] public MhdCard Parent { get; set; } = default!;
    [Parameter] public RenderFragment? ChildContent { get; set; }

    protected override void OnInitialized()
    {
        Parent.SetBody(ChildContent);
    }
}
