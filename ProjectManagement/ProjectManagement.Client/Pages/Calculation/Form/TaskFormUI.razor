@using BlazorMHD.UI.Components.Data.Card
@using ProjectManagement.Client.Shared.MVVM.Calculation
@using ProjectManagement.Client.Shared.Model.Project.Calculation;
@using ProjectManagement.Client.Shared.Repositories.Calculation
@using ProjectManagement.Shared.Base.Calculation
@using ProjectManagement.Shared.DTO.Calculation
@attribute [Authorize(Roles = PMRolesConst.Tenant.AdminManger)]
@inherits AppComponentBase


<EditForm id="taskForm"
          Model="@TaskUpdate"
          OnValidSubmit="async()=> await HandleSubmitAsync()">

    @if (Task.Id == 0 && PostList.Count > 1)
    {
        <div>
            <ul class="nav nav-pills mb-3" id="pills-tab" role="tablist">
                @for (int i = 0; i < PostList.Count; i++)
                {
                    int x = i;
                    <li class="nav-item" role="presentation">
                        <button @onclick="()=> TaskUpdate = PostList[x]" class="nav-link active" type="button" role="tab">@(i + 1)</button>
                    </li>
                }
            </ul>
        </div>
    }

    <DataAnnotationsValidator />

    <MhdCard Elevated="true" Compact="true" Class="mb-2">
        <div class="@CssClasses.Layout.FlexWrap">
            <div class="@CssClasses.Layout.FlexWidth(200)">
                <label class="@CssClasses.Form.Label">@CalcResource.code</label>
                <InputText @bind-Value="TaskUpdate.Data.Code" class="@CssClasses.Input.Text" />
                <ValidationMessage For="@(() => TaskUpdate.Data.Code)" />
            </div>
            <div class="@CssClasses.Layout.FlexMinWidth(400)">
                <label class="@CssClasses.Form.Label">@CalcResource.name </label>
                <InputText @bind-Value="TaskUpdate.Name" class="@CssClasses.Input.Text" />
                <ValidationMessage For="@(() => TaskUpdate.Name)" />
            </div>
        </div>
    </MhdCard>

    <MhdCard Elevated="true" Compact="true" Class="mb-2">
        <div class="@CssClasses.Layout.FlexWrap">
            @if (!TaskUpdate.OnlyCodeText)
            {
                <div class="@CssClasses.Layout.FlexWidth(InputNumber + (TaskUpdate.Data.QuantityParam == ConstValues.FixedQ ? 130 : 0))">
                    <label class="@CssClasses.Form.Label">@CalcResource.quantity</label>
                    <div class="w-full @CssClasses.Layout.ButtonGroup">
                        <InputSelect class="@CssClasses.Input.Select"
                                     @bind-Value:get="TaskUpdate.Data.QuantityParam"
                                     @bind-Value:set="(value) => ChangeQuantityParam(value)">
                            <option></option>
                            <option value="@ConstValues.FixedQ">FixedQ</option>
                            @if (Calc.QuanityList != null)
                                foreach (var status in Calc.QuanityList)
                                {
                                    <option value="@status.Name">@status.Name (@status.Quantity)</option>
                                }
                        </InputSelect>

                        @if (TaskUpdate.Data.QuantityParam == ConstValues.FixedQ)
                        {
                            <InputNumber @bind-Value="TaskUpdate.Data.Quantity" class="@CssClasses.Input.Text" />
                        }
                    </div>
                </div>

                <div class="@CssClasses.Layout.FlexWidth(InputNumber)">
                    <label class="@CssClasses.Form.Label">@CalcResource.unit</label>
                    <InputText @bind-Value="TaskUpdate.Data.Unit" class="@CssClasses.Input.Text" />
                    <ValidationMessage For="@(() => TaskUpdate.Data.Unit)" />
                </div>

                @if (Task.Tasks == null || Task.Tasks.Count == 0)
                {
                    <div class="@CssClasses.Layout.FlexWidth(InputNumber)">
                        <label class="@CssClasses.Form.Label">@CalcResource.cap </label>
                        <InputNumber @bind-Value="TaskUpdate.Data.Ca‌​p" class="@CssClasses.Input.Text" />
                        <ValidationMessage For="@(() => TaskUpdate.Data.Cap)" />
                    </div>
                }

                @if (TaskUpdate.TaskId.HasValue)
                {
                    <div class="@CssClasses.Layout.FlexWidth(130)">
                        <label class="@CssClasses.Form.Label">@CalcResource.changeFactor1</label>
                        <InputNumber @bind-Value="TaskUpdate.Data.ChangeFactor1" class="@CssClasses.Input.Text" />
                        <ValidationMessage For="@(() => TaskUpdate.Data.ChangeFactor1)" />
                    </div>
                    <div class="@CssClasses.Layout.FlexWidth(130)">
                        <label class="@CssClasses.Form.Label">@CalcResource.changeFactor2 </label>
                        <InputNumber @bind-Value="TaskUpdate.Data.ChangeFactor2" class="@CssClasses.Input.Text" />
                        <ValidationMessage For="@(() => TaskUpdate.Data.ChangeFactor2)" />
                    </div>
                }

                <div class="@CssClasses.Layout.FlexWidth(InputNumber)">
                    <label class="@CssClasses.Form.Label">@CalcResource.priceSub </label>
                    <InputNumber @bind-Value="TaskUpdate.Data.PriceSubDB" class="@CssClasses.Input.Text" />
                    <ValidationMessage For="@(() => TaskUpdate.Data.PriceSubDB)" />
                </div>
                <div class="@CssClasses.Layout.FlexWidth(InputNumber)">
                    <label class="@CssClasses.Form.Label">@CalcResource.minPrice </label>
                    <InputNumber @bind-Value="TaskUpdate.Data.MinPrice" class="@CssClasses.Input.Text" />
                    <ValidationMessage For="@(() => TaskUpdate.Data.MinPrice)" />
                </div>
                <div class="@CssClasses.Layout.FlexWidth(InputNumber)">
                    <label class="@CssClasses.Form.Label">@CalcResource.ceilingPrice </label>
                    <InputNumber @bind-Value="TaskUpdate.Data.CeilingPrice" class="@CssClasses.Input.Text" />
                    <ValidationMessage For="@(() => TaskUpdate.Data.CeilingPrice)" />
                </div>
            }

            @if (Task.Resources == null || Task.Resources.Count == 0 || Task.Data.Type == TaskType.CodeName)
            {
                <div class="@CssClasses.Layout.FlexWidth(150)">
                    <div class="@CssClasses.Form.CheckWrapperM">
                        <InputCheckbox @bind-Value="TaskUpdate.OnlyCodeText" class="@CssClasses.Input.Check" />
                        <label class="@CssClasses.Form.Label">Only Code Text</label>
                    </div>
                </div>
            }
        </div>
    </MhdCard>

    @if (!TaskUpdate.OnlyCodeText)
    {
        <MhdCard Elevated="true" Compact="true" Class="mb-2">
            <div class="@CssClasses.Layout.FlexWrap">
                <div class="@CssClasses.Layout.FlexWidth(Selectwidth)">
                    <label class="@CssClasses.Form.Label">@CalcResource.status</label>
                    <InputSelect @bind-Value="TaskUpdate.StatusId" class="@CssClasses.Input.Select">
                        <option></option>
                        @if (Statuses != null)
                            foreach (var status in Statuses)
                            {
                                <option value="@status.Id">@status.Name</option>
                            }
                    </InputSelect>
                    <ValidationMessage For="@(() => TaskUpdate.StatusId)" />
                </div>

                <div class="@CssClasses.Layout.FlexWidth(Selectwidth)">
                    <label class="@CssClasses.Form.Label">@CalcResource.opportunity</label>
                    <InputSelect @bind-Value="TaskUpdate.OpportunityId" class="@CssClasses.Input.Select">
                        <option></option>
                        @if (Calc.Opportunities != null)
                            foreach (var opportunity in Calc.Opportunities)
                            {
                                <option value="@opportunity.Id">@opportunity.OpportunitiesRisks @opportunity.Type</option>
                            }
                    </InputSelect>
                    <ValidationMessage For="@(() => TaskUpdate.OpportunityId)" />
                </div>

                <div class="@CssClasses.Layout.FlexWidth(250)">
                    <label class="@CssClasses.Form.Label">@CalcResource.responsible</label>
                    <InputText @bind-Value="TaskUpdate.Data.Responsible" class="@CssClasses.Input.Text" />
                    <ValidationMessage For="@(() => TaskUpdate.Data.Responsible)" />
                </div>

                <div class="@CssClasses.Layout.FlexWidth(InputNumber)">
                    <div class="@CssClasses.Form.CheckWrapperM">
                        <InputCheckbox class="@CssClasses.Input.Check" @bind-Value="TaskUpdate.Data.PriceSubInPrecent" />
                        <label class="@CssClasses.Form.Label">
                            @ResourceLoc.inprecent
                        </label>
                    </div>
                </div>

                <div class="@CssClasses.Layout.FlexWidth(InputNumber)">
                    <div class="@CssClasses.Form.CheckWrapperM">
                        <InputCheckbox @bind-Value="TaskUpdate.Data.IsActive" class="@CssClasses.Input.Check" />
                        <label class="@CssClasses.Form.Label">@CalcResource.active</label>
                    </div>
                </div>
            </div>
        </MhdCard>
    }

    <MhdCard Elevated="true" Compact="true" Class="mb-2">
        <div class="@CssClasses.Layout.FlexWrap">
            <div class="flex-grow">
                <label class="@CssClasses.Form.Label">@CalcResource.note </label>
                <InputTextArea @bind-Value="TaskUpdate.Data.Note" class="@CssClasses.Input.Text" />
                <ValidationMessage For="@(() => TaskUpdate.Data.Note)" />
            </div>
            <div class="w-[100px]">
                <button type="button"
                        class="@CssClasses.Btn.Solid.Primary mt-7"
                        @onclick="() => TaskUpdate.Data.UpperNote.Add(string.Empty)">
                    @CalcResource.UpperNote
                </button>
            </div>
        </div>

        @for (int i = 0; i < TaskUpdate.Data.UpperNote.Count; i++)
        {
            int d = i;
            <div class="@CssClasses.Layout.FlexWrap mt-1">
                <div class="flex-grow">
                    <textarea lang="3" @bind="TaskUpdate.Data.UpperNote[d]" class="@CssClasses.Input.Text"></textarea>
                </div>
                <div class="w-[100px]">
                    <button type="button"
                            class="@CssClasses.Btn.Solid.Danger"
                            @onclick="() => TaskUpdate.Data.UpperNote.RemoveAt(d)">
                        @((MarkupString)@Icons.DeleteSVG)
                    </button>
                </div>
            </div>
        }
    </MhdCard>
</EditForm>


<div class="@CssClasses.Modal.Footer">
    @if (Task.Id == 0)
    {
        if (PostList.Count > 1)
        {
            <button type="button" class="@CssClasses.Btn.Outline.Danger" @onclick="()=> PostList.Remove(TaskUpdate)">@ResourceApp.delete</button>
        }
        <button type="button" class="@CssClasses.Btn.Outline.Primary" @onclick="NewSP">@ResourceApp.newItem</button>
    }
    <button type="submit" class="@CssClasses.Btn.Outline.Success" form="taskForm" disabled="@btnSubmitDisabled">
        @ResourceApp.save
    </button>
    <button type="button" class="@CssClasses.Btn.Outline.Secondary" @onclick="Close">@ResourceApp.cancel</button>
</div>


@code {
    int Selectwidth = 160;
    int InputNumber = 120;
    List<TaskPostDTO> PostList = new();
    bool btnSubmitDisabled = false;
    List<ListOrderDTO>? Statuses;
    [Parameter] public TaskListMVVM Task { get; set; }
    TaskPostDTO TaskUpdate = new();

    void NewSP()
    {
        TaskPostDTO p = new();
        TaskUpdate.CopyPropertiesTo(p);
        PostList.Insert(0, p);
    }

    private void ChangeQuantityParam(string qp)
    {
        TaskUpdate.Data.QuantityParam = qp;
        if (string.IsNullOrEmpty(qp)) TaskUpdate.Data.Quantity = null;
        else if (qp != ConstValues.FixedQ) TaskUpdate.Data.Quantity = Calc.QuanityList?.FirstOrDefault(x => x.Name == qp)?.Quantity;
    }

    protected async override Task OnInitializedAsync()
    {
        PropertyCopier.CopyPropertiesTo(Task, TaskUpdate);
        TaskUpdate.OnlyCodeText = TaskUpdate.Data.Type == TaskType.CodeName;

        if (Task.Id == 0) PostList.Add(TaskUpdate);
        Statuses = (await ExHandlers.RunCheckTokenAsync(() => Repo.Task.GetAsync())).ToList();
        if(Calc.Opportunities== null)
            Calc.Opportunities = await Repo.Opportunity.GetAsync(Calc.Id);
    }

    void UpdateStatus(TaskListMVVM task)
    {
        var st = Statuses?.FirstOrDefault(x => x.Id == TaskUpdate.StatusId);
        task.Status = st?.Name;
        task.StatusColor = st?.Color;
    }

    void CheckTaskType(TaskPostDTO po)
    {
        if (po.OnlyCodeText)
        {
            po.Data.Type = TaskType.CodeName;
            po.Data.QuantityParam = string.Empty;
            po.Data.Quantity = null;
        }
        else po.Data.Type = TaskType.Task;
    }

    async Task HandleSubmitAsync()
    {
        btnSubmitDisabled = true;
        bool hasSuccess = false;
        try
        {
            if (Task.Id > 0)
            {
                CheckTaskType(TaskUpdate);
                hasSuccess = await ExHandlers.RunCheckTokenAsync(() => Repo.Task.UpdateAsync(TaskUpdate, Task.Id));
            }
            else
            {
                double maxOrder = 0;
                if (Calc.Tasks.Where(x => x.TaskId == Task.TaskId).Any())
                    maxOrder = Calc.Tasks.OrderByDescending(x => x.Order).First().Order + 100;
                foreach (var item in PostList)
                {
                    CheckTaskType(item);
                    item.Order = maxOrder;
                    maxOrder += 100;
                    item.Data.IsOH = Calc.OHFactors;
                }
                hasSuccess = await ExHandlers.RunCheckTokenAsync(() => Repo.Task.CreateAsync(PostList, Calc.Id));
            }
            MHD.Notifications(Task.Id == 0 ? ToastType.Add : ToastType.Update, hasSuccess);
        }
        catch (Exception ex)
        {
            Console.WriteLine(ex.Message);
        }
        Close();
    }

    void Close() => Modal.Close();
}
