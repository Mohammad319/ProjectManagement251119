@inherits AppComponentBase

<EditForm id="projectStatusForm" class="@CssClasses.Modal.Body" Model="@StatusUpdate" OnValidSubmit="HandleSubmitAsync">
    <DataAnnotationsValidator />

    <div class="flex flex-wrap items-center gap-4">
        <div class="flex flex-col">
            <label class="@CssClasses.Form.Label">@CalcLoc[nameof(CalcResource.name)]</label>
            <InputText class="@CssClasses.Input.Text" @bind-Value="@StatusUpdate.Name" />
            <ValidationMessage For="@(() => StatusUpdate.Name)" />
        </div>

        <div class="@CssClasses.Form.CheckWrapperM">
            <InputCheckbox id="projectStatusVisible" class="@CssClasses.Input.Check" @bind-Value="@StatusUpdate.IsVisible" />
            <label class="@CssClasses.Form.Label" for="projectStatusVisible">@AppLoc[nameof(ResourceApp.isVisible)]</label>
        </div>

        <div class="flex flex-col">
            <label class="@CssClasses.Form.Label">@Loc[nameof(ResourceLoc.color)]</label>
            <InputText type="color" class="@CssClasses.Input.Color" @bind-Value="StatusUpdate.Color" />
        </div>
    </div>
</EditForm>

<div class="@CssClasses.Modal.Footer">
    <button type="submit" class="@CssClasses.Btn.Outline.Success" form="projectStatusForm" disabled="@IsLoading">
        @AppLoc[nameof(ResourceApp.save)]
    </button>
    <button type="button" class="@CssClasses.Btn.Outline.Secondary" @onclick="CloseModal" disabled="@IsLoading">
        @AppLoc[nameof(ResourceApp.cancel)]
    </button>
</div>
