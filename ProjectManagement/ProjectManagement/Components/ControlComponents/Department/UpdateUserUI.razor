@using Domain.DTO.User
@using ProjectManagement.Client.Shared.Constants
@using ProjectManagement.Shared
@inherits AppComponentBase

@if (IsLoading)
{
    <div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm dark:border-slate-700 dark:bg-slate-900">
        <BlazorMHD.UI.Components.Feedback.Loading.MhdLoading />
    </div>
}
else if (Form is not null)
{
    <div class="space-y-4">
        <div class="rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm text-slate-600 dark:border-slate-700 dark:bg-slate-800/70 dark:text-slate-300">
            @if (UserForm.Id == 0)
            {
                <span>@WebLoc["RegisterUserHint"]</span>
            }
            else
            {
                <span>@WebLoc["EditUserHint"]</span>
            }
        </div>

        <EditForm Model="Form" OnValidSubmit="HandleSubmitAsync" id="@DialogFormId" class="space-y-4">
            <DataAnnotationsValidator />
            <ValidationSummary class="rounded-xl border border-rose-200 bg-rose-50 p-3 text-xs text-rose-700 dark:border-rose-900/60 dark:bg-rose-950/30 dark:text-rose-300" />

            <div class="grid gap-4 md:grid-cols-2 xl:grid-cols-3">
                <div>
                    <label class="mb-1 block text-sm font-medium text-slate-700 dark:text-slate-200">@ResourceIdentity.firstname</label>
                    <InputText class="w-full rounded-xl border border-slate-300 bg-white px-3 py-2 text-sm text-slate-900 shadow-sm outline-none transition focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 disabled:cursor-not-allowed disabled:opacity-60 dark:border-slate-700 dark:bg-slate-950 dark:text-slate-100"
                               @bind-Value="Form.Firstname"
                               disabled="@IsBusy" />
                    <ValidationMessage For="() => Form.Firstname" class="mt-1 block text-xs text-rose-600 dark:text-rose-300" />
                </div>

                <div>
                    <label class="mb-1 block text-sm font-medium text-slate-700 dark:text-slate-200">@ResourceIdentity.lastname</label>
                    <InputText class="w-full rounded-xl border border-slate-300 bg-white px-3 py-2 text-sm text-slate-900 shadow-sm outline-none transition focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 disabled:cursor-not-allowed disabled:opacity-60 dark:border-slate-700 dark:bg-slate-950 dark:text-slate-100"
                               @bind-Value="Form.Lastname"
                               disabled="@IsBusy" />
                    <ValidationMessage For="() => Form.Lastname" class="mt-1 block text-xs text-rose-600 dark:text-rose-300" />
                </div>

                <div>
                    <label class="mb-1 block text-sm font-medium text-slate-700 dark:text-slate-200">@ResourceIdentity.email</label>
                    <InputText class="w-full rounded-xl border border-slate-300 bg-white px-3 py-2 text-sm text-slate-900 shadow-sm outline-none transition focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 disabled:cursor-not-allowed disabled:opacity-60 dark:border-slate-700 dark:bg-slate-950 dark:text-slate-100"
                               @bind-Value="Form.Email"
                               disabled="@IsBusy" />
                    <ValidationMessage For="() => Form.Email" class="mt-1 block text-xs text-rose-600 dark:text-rose-300" />
                </div>

                <div>
                    <label class="mb-1 block text-sm font-medium text-slate-700 dark:text-slate-200">@ResourceIdentity.phone</label>
                    <InputText class="w-full rounded-xl border border-slate-300 bg-white px-3 py-2 text-sm text-slate-900 shadow-sm outline-none transition focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 disabled:cursor-not-allowed disabled:opacity-60 dark:border-slate-700 dark:bg-slate-950 dark:text-slate-100"
                               @bind-Value="Form.PhoneNumber"
                               disabled="@IsBusy" />
                    <ValidationMessage For="() => Form.PhoneNumber" class="mt-1 block text-xs text-rose-600 dark:text-rose-300" />
                </div>

                <div>
                    <label class="mb-1 block text-sm font-medium text-slate-700 dark:text-slate-200">@PMResourceIdentity.role</label>
                    <InputSelect class="w-full rounded-xl border border-slate-300 bg-white px-3 py-2 text-sm text-slate-900 shadow-sm outline-none transition focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 disabled:cursor-not-allowed disabled:opacity-60 dark:border-slate-700 dark:bg-slate-950 dark:text-slate-100"
                                 @bind-Value="Form.Role"
                                 disabled="@IsBusy">
                        <option value="@PMRolesConst.Tenant.Admin">@ResourceIdentity.Admin</option>
                        <option value="@PMRolesConst.Tenant.Manger">@PMResourceIdentity.SuperManger</option>
                        <option value="@PMRolesConst.Tenant.Manger">@ResourceIdentity.Manger</option>
                        <option value="@PMRolesConst.Tenant.User">@ResourceIdentity.User</option>
                    </InputSelect>
                </div>

                @if (Departments?.Any() == true && Form.Role != PMRolesConst.Tenant.Admin)
                {
                    <div>
                        <label class="mb-1 block text-sm font-medium text-slate-700 dark:text-slate-200">@ResourceIdentity.department</label>
                        <InputSelect class="w-full rounded-xl border border-slate-300 bg-white px-3 py-2 text-sm text-slate-900 shadow-sm outline-none transition focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 disabled:cursor-not-allowed disabled:opacity-60 dark:border-slate-700 dark:bg-slate-950 dark:text-slate-100"
                                     @bind-Value="Form.DepartmentId"
                                     disabled="@IsBusy">
                            @foreach (var dep in Departments)
                            {
                                <option value="@dep.Id">@dep.Name</option>
                            }
                        </InputSelect>
                        <ValidationMessage For="() => Form.DepartmentId" class="mt-1 block text-xs text-rose-600 dark:text-rose-300" />
                    </div>
                }

                <div>
                    <label class="mb-1 block text-sm font-medium text-slate-700 dark:text-slate-200">@PMResourceIdentity.lockoutStart</label>
                    <div class="flex items-center gap-2">
                        <InputDate class="w-full rounded-xl border border-slate-300 bg-white px-3 py-2 text-sm text-slate-900 shadow-sm outline-none transition focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 disabled:cursor-not-allowed disabled:opacity-60 dark:border-slate-700 dark:bg-slate-950 dark:text-slate-100"
                                   @bind-Value="Form.LockoutStart"
                                   disabled="@IsBusy" />
                        <button class="inline-flex items-center justify-center rounded-xl border border-rose-300 bg-rose-50 px-3 py-2 text-sm font-medium text-rose-700 transition hover:bg-rose-100 disabled:cursor-not-allowed disabled:opacity-60 dark:border-rose-900/60 dark:bg-rose-950/30 dark:text-rose-300 dark:hover:bg-rose-950/50"
                                type="button"
                                disabled="@IsBusy"
                                @onclick="ClearLockoutStart">
                            @((MarkupString)Icons.DeleteSVG)
                        </button>
                    </div>
                    <ValidationMessage For="() => Form.LockoutStart" class="mt-1 block text-xs text-rose-600 dark:text-rose-300" />
                </div>

                <div>
                    <label class="mb-1 block text-sm font-medium text-slate-700 dark:text-slate-200">@PMResourceIdentity.lockoutEnd</label>
                    <div class="flex items-center gap-2">
                        <InputDate class="w-full rounded-xl border border-slate-300 bg-white px-3 py-2 text-sm text-slate-900 shadow-sm outline-none transition focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 disabled:cursor-not-allowed disabled:opacity-60 dark:border-slate-700 dark:bg-slate-950 dark:text-slate-100"
                                   @bind-Value="Form.LockoutEnd"
                                   disabled="@IsBusy" />
                        <button class="inline-flex items-center justify-center rounded-xl border border-rose-300 bg-rose-50 px-3 py-2 text-sm font-medium text-rose-700 transition hover:bg-rose-100 disabled:cursor-not-allowed disabled:opacity-60 dark:border-rose-900/60 dark:bg-rose-950/30 dark:text-rose-300 dark:hover:bg-rose-950/50"
                                type="button"
                                disabled="@IsBusy"
                                @onclick="ClearLockoutEnd">
                            @((MarkupString)Icons.DeleteSVG)
                        </button>
                    </div>
                    <ValidationMessage For="() => Form.LockoutEnd" class="mt-1 block text-xs text-rose-600 dark:text-rose-300" />
                </div>

                <div class="flex items-center gap-2 rounded-xl border border-slate-200 bg-slate-50 px-3 py-2 dark:border-slate-700 dark:bg-slate-800/60">
                    <InputCheckbox class="h-4 w-4 rounded border-slate-300 text-blue-600 focus:ring-blue-500 dark:border-slate-600 dark:bg-slate-900"
                                   @bind-Value="Form.PhoneNumberConfirmed"
                                   disabled="@IsBusy" />
                    <label class="text-sm text-slate-700 dark:text-slate-200">@PMResourceIdentity.phoneNumberConfirmed</label>
                </div>

                <div class="flex items-center gap-2 rounded-xl border border-slate-200 bg-slate-50 px-3 py-2 dark:border-slate-700 dark:bg-slate-800/60">
                    <InputCheckbox class="h-4 w-4 rounded border-slate-300 text-blue-600 focus:ring-blue-500 dark:border-slate-600 dark:bg-slate-900"
                                   @bind-Value="Form.LockoutEnabled"
                                   disabled="@IsBusy" />
                    <label class="text-sm text-slate-700 dark:text-slate-200">@PMResourceIdentity.logout</label>
                </div>
            </div>
        </EditForm>

    </div>
}
